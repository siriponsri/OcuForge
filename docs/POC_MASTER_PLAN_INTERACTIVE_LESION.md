# Superseded interactive-lesion plan pointer

This earlier plan is historical only. It does not define the current phase, model selection, or customer workflow.

Use [POC_MASTER_PLAN.md](POC_MASTER_PLAN.md). The current direction keeps global DR grading and ROI lesion
classification as separate tracks and integrates the existing `templates/` DR Review Workspace.
