[tool.pytest.ini_options]
testpaths = ["eyes-detected-contracts/tests", "eyes-detected-models/tests", "eyes-detected-labeler/tests", "tests"]
addopts = "-q"
markers = ["external: external service or model weights required"]
[tool.ruff]
line-length = 110
[tool.ruff.lint]
select = ["E9", "F"]
